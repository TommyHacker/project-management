/** @format */

const Profile = ({ user }) => {
	return (
		<div className='page-container'>
			<h1 style={{ textTransform: 'capitalize' }}>Profile: {user}</h1>
		</div>
	);
};

export default Profile;
